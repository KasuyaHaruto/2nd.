# 災害対策システム再現用プロンプト

以下の指示を1回の依頼として実行し、動作確認まで完了してください。質問や段階的な提案で止まらず、プロジェクト一式を実装してください。

## 目的

Flaskで、日本語の住民向け災害対策システムを作成してください。完成形は、避難所情報、気象警報、管理者からの指示・発信を扱えるレスポンシブなWebアプリケーションです。画面文言、ルート、データ形式、認証、現在のUI挙動は、以下の仕様を優先してください。

## 技術・構成

- Python 3、Flask 3.0.3を使用する。
- ルート直下に `app.py`、`requirements.txt`、`vercel.json`、`tests/` を置く。
- `bousai_app/templates/` に以下を作成する。
  - `base.html`
  - `index.html`
  - `login.html`
  - `board.html`
  - `shelter_register.html`
  - `shelter_search.html`
  - `search_results.html`
- `bousai_app/data/` に以下のJSONファイルを置く。
  - `shelters.json`
  - `instructions.json`
  - `notification_history.json`
- 画像は `bousai_app/static/uploads/` に保存する。
- 共通レイアウトは `base.html` に集約する。
- JSONが存在しない、空、または壊れている場合は空配列へフォールバックする。
- `requirements.txt` には少なくとも `flask==3.0.3` を含める。地図はLeafletとOpenStreetMapをCDNから読み込む。
- `vercel.json` では `app.py` をPythonエントリーポイントとして扱えるようにする。

## 起動方法

以下で起動できるようにしてください。

```bash
pip install -r requirements.txt
python app.py
```

アプリは `http://localhost:5000` で動作し、`app.py` の直接起動時はポート5000、デバッグ有効とします。

## 共通デザイン

- HTMLの言語は日本語（`lang="ja"`）、タイトルは「防災アプリ」とする。
- 青系を基調とした固定ヘッダーを表示する。
- ヘッダーには「防災アプリ」ブランド名と折りたたみ式ハンバーガーメニューを置く。
- メニューにはホーム、避難所検索、避難所登録、指示・発信ボードを置く。
- 未ログイン時は「未ログイン」「ログイン」、ログイン時はユーザー名と「ログアウト」を表示する。
- 最大幅約980pxの中央レイアウトとし、スマートフォンでも崩れないレスポンシブデザインにする。
- ボタン、フォーム、警告表示、モーダル、履歴一覧は視認性とキーボード操作を考慮する。
- 既存の画面を別AIで再現する場合、機能を省略せず、以下の表示状態と操作を実装する。

## 認証

- 管理者の認証情報はユーザー名 `admin`、パスワード `123` とする。
- `/shelter_register` と `/board` はログイン必須にする。
- 未ログインで保護ページへアクセスした場合は `/login?next=元URL` へリダイレクトする。
- ログイン成功後は、外部サイトへ遷移しない安全な `next` URLであれば元ページへ戻す。
- `/logout` ではセッションを破棄する。
- ログイン画面にはパスワード `123` を説明として表示する。
- 認証失敗時は「パスワードが正しくありません。」と表示する。

## ルートと画面

### `/` ホーム

- Leaflet/OpenStreetMapによる「避難所マップ」を表示する。
- 地図の初期座標は `[35.3390, 139.4903]`、ズームは12とする。現行仕様では青森市の気象情報と地図座標が一致しないが、そのまま再現する。
- 現状は地図上に避難所マーカーを表示せず、`/shelters` APIとも接続しない。
- 「青森市 気象警報・注意報」を表示する。
- 初回表示時に `/api/weather_warnings` を取得し、その後10分ごとに自動更新する。
- 手動更新ボタンを設ける。
- 「警報あり」「警報なし」「取得失敗」の状態を表示する。

### `/login`

- パスワード入力フォーム、`next` hiddenフィールド、エラー表示を持つ。
- ログイン成功後は安全な `next` の遷移先、なければホームへ移動する。

### `/shelter_register`

- 管理者専用ページにする。
- 避難所名の必須入力フォームを表示する。
- 空入力時は「新しい避難所情報を登録します。避難所名を入力してください。」と表示し、登録しない。
- 成功時は「避難所を登録しました。」と表示する。
- IDは既存の最大IDに1を加えて採番する。
- `{ "id": 数値, "name": 文字列 }` の形式で `shelters.json` に保存する。

### `/shelter_search`、`/all_shelters`、`/search_results`

- 検索画面には避難所検索の説明と「全施設一覧」リンクを表示する。
- `/all_shelters` では全避難所を表示する。
- `/search_results?district=値` では `district` 条件で絞り込む。
- 結果は避難所名の箇条書きで表示する。
- 結果がない場合は該当なしメッセージを表示する。
- 初期データは `id` と `name` を基本フィールドとする。地区フィールドがない場合、地区指定検索は一致なしになる現行挙動を維持する。

### `/board`

管理者専用の指示・発信ボードを作成してください。

- GETで画面を表示し、POSTで発信を登録する。
- 送信先を選べるチェックボックスを置く。
  - 全住民
  - 北部のみ
  - 南部のみ
  - 庁内・消防団
- 発信手段を選べるチェックボックスを置く。
  - アプリ
  - 屋外スピーカー
  - 戸別受信機
  - SNS
- 緊急度のラジオボタンを置く。
  - 即時避難
  - 警戒・準備
  - お知らせ
- 自由入力メッセージ欄と画像添付欄を置く。
- 発信前に内容を確認できるプレビューモーダルを表示する。
- 登録成功時は「発信が登録されました。」と表示する。
- targetの保存値は次のように変換する。
  - 全住民 → 全住民
  - 北部のみ → 北部
  - 南部のみ → 南側
  - 庁内・消防団 → 庁内
- statusの保存値は次のように変換する。
  - 即時避難 → `🔴即時避難`
  - 警戒・準備 → `🟡警戒・準備`
  - お知らせ → `🔵お知らせ`
- IDは既存最大IDに1を加えて採番する。
- 作成日時と更新日時は日本時間（JST）で保存する。
- 新しい発信は `instructions.json` の先頭へ追加して保存する。
- 画像の許可拡張子は png、jpg、jpeg、gif、webp とする。
- 画像は `static/uploads` に保存し、履歴から表示できるURLを保存する。
- 発信履歴は3件単位でページングし、`page=1`、`page=2` を扱う。
- 履歴検索を実装し、履歴項目をクリックすると内容と画像を確認できるようにする。
- 添付画像はクリックでライトボックス表示し、Escapeキーで閉じる。
- 文字サイズ変更機能を実装する。
  - 大: 1.18倍
  - 中: 1倍
  - 小: 0.9倍
- 更新ボタンで画面上の最終更新時刻を更新する。
- 言語選択、文字読み上げボタン、「新規発信」ボタン、通信状態表示はUIとして表示する。現行仕様では実処理がなくてもよい。

## JSONデータ

`shelters.json` は次の配列形式にする。

```json
[
  { "id": 1, "name": "避難所名" }
]
```

`instructions.json` は次のフィールドを基本とする。

```json
[
  {
    "id": 1,
    "target": "全住民",
    "content": "発信内容",
    "shelter": "対象避難所または空文字",
    "status": "🔵お知らせ",
    "created_at": "JSTの日時",
    "updated_at": "JSTの日時",
    "channels": ["アプリ"],
    "urgency": "お知らせ",
    "image_url": "アップロード画像URLまたは空文字"
  }
]
```

`instructions.json` には画面確認用の履歴データを30件程度用意し、画像付きデータ、全住民、北部、南側などの例を含める。`notification_history.json` には気象警報履歴を3件程度用意するが、現行アプリでは読み込まず画面にも表示しない。

## 気象庁API

`/api/weather_warnings` を実装してください。

- 取得先は `https://www.jma.go.jp/bosai/warning/data/r8/020000.json` とする。
- 青森県コードは `020000`、対象地域の市区町村コードは `0220100` とする。
- 新形式のレスポンス配列から `warning.class20Items` を探す。
- `areaCode` が `0220100` の要素だけを処理する。
- `status` が「発表」または「継続」の警報・注意報だけを返す。
- 同じ警報コードは重複排除する。
- 警報コードを名称へ変換し、未知のコードはコード番号付きの名称にする。
- コード10は「レベル2大雨注意報」として扱う。
- `reportDatetime` はJST表示へ変換する。
- 取得失敗時はHTTPエラーにせず、`error: true`、`warnings: []`、`report_time: "取得失敗"` を返す。

## API

`GET /shelters` を実装してください。

- `district` クエリで避難所を絞り込む。
- 該当する避難所をJSONで返す。
- 該当なしの場合はHTTP 404と `{ "error": "No shelters found" }` を返す。

## テスト

`tests/test_shelter_register.py` を作成し、unittestまたはpytestで実行できるようにする。少なくとも以下を検証する。

- 登録画面がHTTP 200で表示され、指定文言が含まれる。
- 避難所登録成功メッセージが表示される。
- ボードの主要コントロール、プレビュー、画像、履歴検索が表示される。
- 履歴の3件単位ページングで1ページ目と2ページ目が動作する。
- 発信内容と画像URLが保存される。
- 気象警報の解析関数が対象地域コード `0220100` を処理する。
- 警報コード10を「レベル2大雨注意報」として扱う。

テスト実行時に実データを壊さないよう、可能なら一時ディレクトリやモックを使う。ただし、既存仕様との互換性を優先する。

## 完了条件

実装後、次を実行してエラーを解消してください。

```bash
pytest -q
```

最後に、作成・変更したファイル、起動方法、テスト結果を簡潔に報告してください。機能を削除したり、仕様を別の設計へ置き換えたりせず、上記の現行挙動を完成形として再現してください。
